# Example environment variables file (do NOT commit real secrets)
# Copy this to `.env` and fill values locally. This file is safe to commit because it contains only placeholders.
GROQ_API_KEY=your-groq-api-key-here
OPENAI_API_KEY=your-openai-api-key-here

# Optional: other config
DEBUG=false